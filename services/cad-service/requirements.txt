fastapi==0.110.0
uvicorn==0.27.1
pydantic==2.6.3
cadquery==2.4.0
minio==7.2.5
trimesh==4.4.1
numpy==1.26.4
pyglet==2.0.15
httpx==0.27.0

# CAD-Coder local model inference
# NOTE: torch is installed with CUDA wheels in the Dockerfile before this file
# is processed. Do NOT list torch/torchvision here — the Dockerfile handles them
# separately with --extra-index-url to get the cu121 builds.
# torch==2.3.1+cu121          <- installed in Dockerfile RUN step
# torchvision==0.18.1+cu121   <- installed in Dockerfile RUN step
transformers==4.44.2
accelerate==0.33.0
bitsandbytes==0.43.3
huggingface_hub==0.24.6
safetensors==0.4.4

langgraph
langchain
langchain-core
langchain-community
openai
prompt-toolkit

chromadb
opentelemetry-api
opentelemetry-sdk
tenacity
jinja2
langchain-google-genai
